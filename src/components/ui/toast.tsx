"use client"

// Optional toast.tsx to fulfill exact user request file list, 
// though we usually just use sonner's toast direct export or use-toast.ts
import { toast as sonnerToast } from "sonner"

export { sonnerToast as toast }
